
  // write a function that takes two integers as input and returns their sum
  int sum(int x, int y) {
    return x + y;
  }

  int main() {
    cout << sum(5, 7) << endl; // should print 12
    return 0;
  }